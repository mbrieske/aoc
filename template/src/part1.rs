pub fn solve(input: &str) -> usize {
    let mut lines = input.lines();
    todo!()
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn example() {
        let example = "";

        assert_eq!(solve(example), 0);
    }
}
